import { createTheme } from '@mui/material'

const theme = () => createTheme({
    typography:{
      fontFamily:[
        "Diphylleia", 'serif'
      ].join(',')
    },
    palette:{
      primary: 
    }
    });
export default theme